<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <div class="navbar-header">
      <%# Botón para vista de móvil %>
      <button aria-expanded="false" class="navbar-toggle collapsed" data-target="#opciones-navbar" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%# Logo %>
      <%= link_to 'AECCI', root_path, class: 'navbar-brand' %>
    </div>
    <%# Menú que irá dentro del botón de vista movil %>
    <div class="collapse navbar-collapse" id="opciones-navbar">
      <%# Menú de la derecha %>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to '¿Quiénes somos?', about_us_path %>
        </li>
        <li>
          <%= link_to 'Anuncios', ads_path %>
        </li>
        <% if user_signed_in? %>
            <div class="dropdown">
              <button class="btn dropdown-toggle" type="button" id="dropdown-opciones" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                Opciones <span class="caret"></span>
              </button>
              <ul class="dropdown-menu" aria-labelledby="dropdown-opciones">
                <li><%= link_to 'Editar perfil', edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Cerrar sesión', destroy_user_session_path %></li>
              </ul>
            </div>
        <% else %>
            <li><%= button_to 'Iniciar Sesión', new_user_session_path, method: :get, class: 'btn navbar-btn btn-login' %></li>
            <li><%= button_to 'Registrarse', new_user_registration_path, method: :get, class: 'btn navbar-btn btn-registro' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
