<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <div class="navbar-header">
      <%# Botón para vista de móvil %>
      <button aria-expanded="false" class="navbar-toggle collapsed" data-target="#opciones-navbar" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%# Logo %>
      <%= link_to image_tag('logo_aecci.png', class: 'logo'), root_path, class: 'navbar-brand' %>
    </div>
    <%# Menú que irá dentro del botón de vista movil %>
    <div class="collapse navbar-collapse" id="opciones-navbar">
      <%# Menú de la derecha %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Anuncios', ads_path %></li>
        <% if user_signed_in? %>
            <li><%= link_to 'Banco de exámenes', courses_path %>
            </li>
            <% if current_user.admin? %>
                <li><%= link_to 'Usuarios', users_path %></li>
            <% end %>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                  <small><i class="fa fa-sign-out"></i></small> Cerrar sesión
              <% end %>
            </li>
        <% else %>
            <li><%= button_to 'Ingresar', new_user_session_path, method: :get, class: 'btn navbar-btn btn-login btn-sm' %></li>
            <li><%= button_to 'Registrarse', new_user_registration_path, method: :get, class: 'btn navbar-btn btn-registro btn-sm' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
